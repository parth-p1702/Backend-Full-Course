// module:- is a reusable block of code that encapsulates related functionality.
// It can be imported and used in other parts of the application, promoting code reusability and organization.

let math = (a,b) => {
  return a + b;
}
export default math;